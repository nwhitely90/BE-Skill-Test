<p>
  <strong>Name:</strong>
  <br>
  <%= @site.name %>
</p>
 
<p>
  <strong>Explanation:</strong>
  <br>
  <%= @site.explanation %>
</p>

<p>
  <strong>Active:</strong>
  <br>
  <% if @site.active == true %>
    有効
   <% else %>
    無効
  <% end %>
</p>

<p>
  <%= link_to "Show products", admin_products_path(:site_id => @site.id) %>
    
  <%= form_for admin_products_path(@site.id) do |f| %>
    <%= f.submit "Products" %> 
  <% end %>
    
</p>
 
<%= link_to 'Edit', edit_admin_site_path(@site) %> |
<%= link_to 'Back', admin_sites_path %>